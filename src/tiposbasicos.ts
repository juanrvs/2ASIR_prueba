/*
ver:
Documentación typescript
*/
let activo: boolean
let valor: number
let cantidad: ( string | number)[]
let nombre: string
let lista: number[]
let edad = 30
activo = false
if (!activo) {
    console.log('No está activo')
} else {
    console.log('Está activo')
}